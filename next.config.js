/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.syncfusion.com', 'https://fakestoreapi.com/'],
  },
};

module.exports = nextConfig;
